-- ============================================================
-- 种子数据（可选）
-- 注意：匿名用户的 ID 由 Supabase Auth 自动生成。
-- RLS 已通过 migration.sql 开启，数据按 auth.uid() 自动隔离。
-- 新用户首次登录时无数据，在应用内创建周期即可。
-- 此文件仅用于在特定用户下插入演示数据（调试用）。
-- ============================================================
-- 示例（按需取消注释并替换 user_id 为实际的 auth.uid()）：
-- INSERT INTO cycles (user_id, name, start_date, is_active)
-- VALUES ('<替换为匿名用户的 auth.uid()>', '初始周期', CURRENT_DATE, TRUE);